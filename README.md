# Cry Baby

An assertion library.

## Arrays

```js
describe('some test suite', function () {
    it('some test', function () {

        // Test an Array for an item.
        will([1, 2, 3]).have(3);
    });
});
```